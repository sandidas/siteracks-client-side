import React, {FC} from 'react';
interface IProps {
    item:IPage
}

const TermSinglePage:FC<IProps> = ({item}) => {
    return (
        <div>
            
        </div>
    );
};

export default TermSinglePage;